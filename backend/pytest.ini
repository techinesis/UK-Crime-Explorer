[pytest]
addopts = -m "not smoke"
markers =
    smoke: end-to-end smoke tests against real data; opt-in via -m smoke. Covers API endpoints, composite math invariants, chat tools, and chat HTTP layer. No LLM, no network.
