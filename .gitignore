# Mac system files
.DS_Store

# Python cache files
__pycache__/
*.pyc

# Virtual environments
.env
.venv/
venv/

# Jupyter notebook checkpoints
.ipynb_checkpoints/

# Large data files - do not upload to GitHub
data/london_crime_by_lsoa.csv
outputs/crime_aggregated_for_app.csv

# Per-filter colored GeoJSON cache generated at runtime — keep the directory
# but not the files. See Round 3 architecture in CLAUDE.md.
static/colored/*.geojson
!static/colored/.gitkeep

.cache

# --- Full-stack rewrite (React + FastAPI) ---
# Backend: cached crime DataFrame snapshot (regenerated from the data client)
data/crime_snapshot.parquet
# Frontend: node + build artifacts
frontend/node_modules/
frontend/dist/
frontend/.vite/
# Python venvs anywhere in the tree
backend/.venv/
