# Lean deps for the Vercel serverless function. NO geopandas/shapely/pyogrio
# (boundaries are static CDN assets) and NO uvicorn (Vercel provides the server).
fastapi
pandas
pyarrow
numpy==1.26.4
pydantic>=2
